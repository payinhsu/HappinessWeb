import React, { PropTypes } from 'react';
import {Link} from 'react-router';

export default class OperationsManagementContent extends React.Component {

	static propTypes = {
		targetPage : React.PropTypes.string.isRequired
	};
	
	render() {
		 return (
		 	<div>
		 	</div>
		 );
	}
}